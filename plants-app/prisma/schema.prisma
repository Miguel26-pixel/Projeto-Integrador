// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:postgres@localhost:5432/mydb?schema=public"
}

model RasperryPI {
  id     Int     @id @default(autoincrement())
  piName String
  ip     String
  plants Plant[]
}

model Plant {
  id        Int         @id @default(autoincrement())
  plantdata PlantData[]
  pi        RasperryPI  @relation(fields: [piID], references: [id])
  piID      Int

  @@unique([id])
}

model PlantData {
  time        DateTime @id @default(now())
  temperature Float
  light       Float
  humidity    Float
  gasConc     Float
  Plant       Plant   @relation(fields: [plantId], references: [id])
  plantId     Int
}
